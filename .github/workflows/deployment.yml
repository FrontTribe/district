name: Deploy District Project

on:
  push:
    branches: [main, development]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 🧩 Step 1: Checkout the repository
      - name: Checkout Repo
        uses: actions/checkout@v4
        with:
          ssh-key: ${{ secrets.SSH_PRIVATE_KEY }}
          persist-credentials: false

      # 🔐 Step 2: Setup SSH key for server connection
      - name: Setup SSH Key for Server
        uses: webfactory/ssh-agent@v0.7.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      # ⚙️ Step 3: Setup PNPM
      - name: Setup PNPM
        uses: pnpm/action-setup@v4
        with:
          version: 10.11.0

      # 🧠 Step 4: Test SSH connection
      - name: Test SSH Connection
        run: ssh -o StrictHostKeyChecking=no deployer@${{ secrets.SERVER_IP }} "echo ✅ Connected to server"

      # 🚀 Step 5: Deploy project
      - name: Deploy to Server
        env:
          BRANCH_NAME: ${{ github.ref_name }}
        run: |
          set -e
          if [ "$BRANCH_NAME" = "main" ]; then
            ssh -o StrictHostKeyChecking=no \
                -o ServerAliveInterval=60 \
                -o ServerAliveCountMax=30 \
                deployer@${{ secrets.SERVER_IP }} "\
                  set -e && \
                  export CI=1 && \
                  export NPM_CONFIG_YES=true && \
                  cd /var/www/district-prod && \
                  git fetch origin main && \
                  git reset --hard origin/main && \
                  npm install --no-audit --no-fund --legacy-peer-deps && \
                  npm run build && \
                  pm2 restart district-prod && \
                  echo \"✅ Deployment completed for main\" \
                "
          elif [ "$BRANCH_NAME" = "development" ]; then
            ssh -o StrictHostKeyChecking=no \
                -o ServerAliveInterval=60 \
                -o ServerAliveCountMax=30 \
                deployer@${{ secrets.SERVER_IP }} "\
                  set -e && \
                  export CI=1 && \
                  export NPM_CONFIG_YES=true && \
                  cd /var/www/district-dev && \
                  git fetch origin development && \
                  git reset --hard origin/development && \
                  npm install --no-audit --no-fund --legacy-peer-deps && \
                  npm run build && \
                  pm2 restart district-dev && \
                  echo \"✅ Deployment completed for development\" \
                "
          else
            echo "⚠️ Deployment skipped: branch $BRANCH_NAME is not configured."
          fi
